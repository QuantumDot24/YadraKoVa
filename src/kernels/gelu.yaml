dtypes: [bf16, fp32, fp16]
block: [256, 1, 1]
grid: elementwise(n)
shared_mem_bytes: 0

op:
  name: Gelu
  inputs: [input]
  output: output
  scalar_init:
    n: "input.numel()"
  checks:
    - "input.is_contiguous()"
  backends: [Custom]